const express = require("express");
const routes = express.Router();
/**
 * PUT /api/adressePharmacie
 * @summary Ajouter une adresse pharmacie
 * @tags Adresse pharmacie
 */
module.exports = routes;
